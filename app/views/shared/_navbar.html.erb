<nav class="navbar bg-light ">
  <% unless current_user %>
    <%= link_to t('navbar.log-in'), user_path, class: 'nav-link' %>
  <%  end %>

  <% if current_user %>
    <div class="nav-user d-flex">
      <div class="nav-user-name nav-link">
        <%= t('navbar.dear') %> <%= current_user.name %>
      </div>
      <%= link_to t('navbar.task-list'), tasks_path, :class => 'nav-link'  %>
    </div>
    <div class="nav-user-action">
      <%= link_to t('navbar.log-out'), logout_path, :class => 'nav-link'  %>
    </div>
  <% end %>
</nav>